# Draft: IEEE Brain 2026 submission (work in progress)

Snapshot for collaborator feedback. **Not final.** Nothing here is locked, treat numbers and
wording as provisional.

## What is here
- `abstract_1page.md` / `.pdf` — the 1-page workshop abstract (the actual submission).
- `report.md` / `.pdf` — a 3-page paper-style draft, the seed for the later journal (THMS) extension.
- `submission_checklist.md` — Google Form field mapping, 100-word program blurb, deadlines.
- `figures/` — `fig1_overview` (used in the 1-pager) and `fig2_geometry`, vector PDF + PNG,
  regenerated by `scripts/make_paper_figures.py`.

## Still to do (current focus)
The claim is settling but the draft needs cleanup before it is submission-ready:
- **Analysis results.** Decoding and RSA numbers come from exploratory runs. They need a clean
  final re-run with provenance (subject counts, seeds, package versions) before we trust them.
- **Argument framing.** Current line: the aggregate/aperiodic-power bias reported for
  reconstruction-based models extends to EEGPT, whose added representation-alignment objective
  does not rescue rhythm specificity. Wording is still being tightened to avoid overclaiming
  (we have not shown EEGPT is better or worse than other models, only that the bias persists).
- **Figures.** Fig 1 panel d is an illustrative schematic (labeled as such); captions, styling,
  and possibly a real average PSD are still being refined.

## Not started yet (secondary, ignore for the poster)
These are for the journal extension, not the September poster. Deliberately out of scope now:
- specparam periodic/aperiodic Figure 3 (real decomposition; replaces the total-power proxy).
- head-to-head RSA vs pure-reconstruction models, cross-subject RSA, a second model, causal test.

## Timeline
Target submission mid-September; workshop final deadline Oct 13, 2026; workshop Nov 11-13,
Washington DC. The full-length THMS journal paper is a separate, later submission.
